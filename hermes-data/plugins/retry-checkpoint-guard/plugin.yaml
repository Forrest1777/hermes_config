name: retry-checkpoint-guard
version: "1.1.0"
description: Prevent blind redispatch of implementation worktrees that contain a checkpoint from a prior run.
provides_hooks:
  - on_kanban_dispatch_tick
