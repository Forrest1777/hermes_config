# thin-orchestrator

TODO 4 / POLISH_DEBUG.

Read-only deterministic state compression for `implementation-orchestrator`.
The `orchestrator_compact_state` tool replaces repeated broad discovery calls
with one bounded snapshot of Kanban, checkpoint/handoff and root-worktree Git facts.
